# Secrets / local config (NEVER commit the real one)
config.json
.env
*.local

# Logs
*.log
traffic.log

# Self-heal state (runtime, not committed)
mimowisper-state.json

# Build output (attached to GitHub Releases instead)
/dist/
/releases/

# Bundled runtimes (shipped in release zip, not in git)
node.exe
ffmpeg.exe

# Dependencies
node_modules/

# OS / editor junk
.DS_Store
Thumbs.db
*.tmp
